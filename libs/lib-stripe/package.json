{
  "name": "@simpd/lib-stripe",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf -rf ./dist && rimraf -rf ./build",
    "compile": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@nestjs/common": "*",
    "@nestjs/core": "*",
    "stripe": "*"
  },
  "peerDependencies": {
    "@nestjs/common": "*",
    "@nestjs/core": "*",
    "stripe": "*"
  }
}