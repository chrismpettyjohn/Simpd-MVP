syntax = "proto3";

package simpd.session;

message Session {
    int64 id = 1;
    string userID = 2;
}

message SessionFindOneInput {
    int64 id = 1;
}

service SessionService {
    rpc GetSession (SessionFindOneInput) returns (Session) {}
}
