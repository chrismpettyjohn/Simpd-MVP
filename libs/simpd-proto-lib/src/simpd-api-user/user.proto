syntax = "proto3";

package simpd.user;

message User {
    int64 id = 1;
    string email = 2;
}

message UserFindOneInput {
    int64 id = 1;
}

service UserService {
    rpc GetUser (UserFindOneInput) returns (User) {}
}
